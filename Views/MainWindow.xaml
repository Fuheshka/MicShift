<Window x:Class="MicShift.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MicShift" 
        Height="480" Width="640"
        Background="{DynamicResource Background}"
        Foreground="{DynamicResource Foreground}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Icon="../logo.png">
    
    <Window.Resources>


        <!-- iOS/Win11 Style Toggle Switch -->
        <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Grid x:Name="Grid" Width="44" Height="22" Cursor="Hand">
                                <Border x:Name="BackgroundBorder" Background="{DynamicResource BorderBrush}" CornerRadius="11" BorderThickness="0"/>
                                <Border x:Name="Thumb" Background="{DynamicResource Foreground}" Width="18" Height="18" CornerRadius="9" HorizontalAlignment="Left" Margin="2,0,0,0"/>
                            </Grid>
                            <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="BackgroundBorder" Property="Background" Value="{DynamicResource AccentColor}"/>
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,2,0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="BackgroundBorder" Property="Background" Value="{DynamicResource ButtonBackground}"/>
                                <Setter TargetName="Thumb" Property="Background" Value="{DynamicResource MutedText}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Flat Modern Progress Bar -->
        <Style TargetType="ProgressBar">
            <Setter Property="Background" Value="{DynamicResource Background}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid x:Name="TemplateRoot">
                            <Border x:Name="PART_Track" Background="{TemplateBinding Background}" CornerRadius="4"/>
                            <Border x:Name="PART_Indicator" HorizontalAlignment="Left" Background="{TemplateBinding Foreground}" CornerRadius="4"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Flat Button -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource Foreground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonHoverBackground}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource ButtonHoverBorder}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonBackground}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource MutedText}"/>
                                <Setter Property="Background" Value="{DynamicResource ButtonBackground}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource ButtonBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Flat Danger Button -->
        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="#DC2626"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#EF4444"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#B91C1C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Dark ComboBox Styling with Flat Popup -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource Background}"/>
            <Setter Property="Foreground" Value="{DynamicResource Foreground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                          BorderBrush="{TemplateBinding BorderBrush}" 
                                          BorderThickness="{TemplateBinding BorderThickness}" 
                                          Background="{TemplateBinding Background}" 
                                          Foreground="{TemplateBinding Foreground}"
                                          Focusable="false"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border" CornerRadius="6" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="30"/>
                                                </Grid.ColumnDefinitions>
                                                <Path x:Name="Arrow" Grid.Column="1" Fill="{DynamicResource MutedText}" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource CardBackground}"/>
                                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource ButtonHoverBorder}"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource Background}"/>
                                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="10,3,30,3" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid x:Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="{DynamicResource Background}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Margin="0,2,0,0"/>
                                    <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ComboBoxItem Styling -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{DynamicResource Background}"/>
            <Setter Property="Foreground" Value="{DynamicResource Foreground}"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" SnapsToDevicePixels="true" CornerRadius="4" Padding="{TemplateBinding Padding}" Margin="2">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource CardBackground}"/>
                                <Setter TargetName="Border" Property="TextElement.Foreground" Value="{DynamicResource AccentColor}"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource CardBackground}"/>
                                <Setter TargetName="Border" Property="TextElement.Foreground" Value="{DynamicResource AccentColor}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource CardBackground}"/>
                                <Setter TargetName="Border" Property="TextElement.Foreground" Value="{DynamicResource AccentColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Sidebar Navigation Buttons -->
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource MutedText}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonHoverBackground}"/>
                                <Setter Property="Foreground" Value="{DynamicResource Foreground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/> <!-- Sidebar -->
            <ColumnDefinition Width="*"/>   <!-- Content Area -->
        </Grid.ColumnDefinitions>

        <!-- Sidebar Panel -->
        <Border Grid.Column="0" Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>   <!-- Logo / Title -->
                    <RowDefinition Height="*"/>      <!-- Nav Links -->
                    <RowDefinition Height="Auto"/>   <!-- Bottom Actions -->
                </Grid.RowDefinitions>

                <!-- Title / Logo -->
                <StackPanel Grid.Row="0" Margin="5,10,0,25">
                    <TextBlock Text="MicShift" FontWeight="Black" FontSize="20" Foreground="{DynamicResource AccentColor}"/>
                    <TextBlock Text="Audio Controller" FontSize="10" Foreground="{DynamicResource MutedText}" Margin="1,2,0,0"/>
                </StackPanel>

                <!-- Navigation Links -->
                <StackPanel Grid.Row="1">
                    <Button x:Name="NavDashboardBtn" Content="Dashboard" Style="{StaticResource SidebarButtonStyle}" Click="OnNavDashboardClick" Margin="0,0,0,5"/>
                    <Button x:Name="NavSettingsBtn" Content="Settings" Style="{StaticResource SidebarButtonStyle}" Click="OnNavSettingsClick"/>
                </StackPanel>

                <!-- Bottom Info and Exit -->
                <StackPanel Grid.Row="2">
                    <TextBlock x:Name="VersionTextBlock" Text="v0.3.0" FontSize="11" Foreground="{DynamicResource MutedText}" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <Button Content="Minimize" Height="30" Click="OnMinimizeToTrayClick" Style="{StaticResource ModernButtonStyle}" Margin="0,0,0,8"/>
                    <Button Content="Exit" Height="30" Click="OnExitClick" Style="{StaticResource DangerButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content Area -->
        <Grid Grid.Column="1" Background="{DynamicResource Background}">
            <TabControl x:Name="MainTabControl" Background="Transparent" BorderThickness="0" Margin="15">
                <TabControl.ItemContainerStyle>
                    <Style TargetType="TabItem">
                        <Setter Property="Visibility" Value="Collapsed"/>
                    </Style>
                </TabControl.ItemContainerStyle>

                <!-- Tab 1: Dashboard -->
                <TabItem>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/> <!-- Header Status Card -->
                            <RowDefinition Height="*"/>    <!-- Levels & Control -->
                        </Grid.RowDefinitions>

                        <!-- Header status -->
                        <Border Grid.Row="0" Margin="0,0,0,15" Padding="12" CornerRadius="8" Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Ellipse x:Name="StatusIndicator" Grid.Column="0" Width="12" Height="12" Fill="#EF4444" VerticalAlignment="Center" Margin="5,0,12,0"/>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Automatic Switching Status" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource Foreground}"/>
                                    <TextBlock x:Name="StatusText" Text="Inactive" FontSize="11" Foreground="{DynamicResource MutedText}"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Levels & Control -->
                        <Border Grid.Row="1" Padding="15" CornerRadius="8" Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                            <StackPanel>
                                <!-- Dropdowns -->
                                <Grid Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="15"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Column="0">
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                            <TextBlock Text="Desk Microphone" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource MutedText}"/>
                                            <TextBlock x:Name="DeskActiveLabel" Text="" FontSize="11" FontWeight="Bold" Margin="8,0,0,0"/>
                                        </StackPanel>
                                        <ComboBox x:Name="DeskMicComboBox" Height="30" SelectionChanged="OnMicSelectionChanged"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2">
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                            <TextBlock Text="Headset Microphone" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource MutedText}"/>
                                            <TextBlock x:Name="HeadsetActiveLabel" Text="" FontSize="11" FontWeight="Bold" Margin="8,0,0,0"/>
                                        </StackPanel>
                                        <ComboBox x:Name="HeadsetMicComboBox" Height="30" SelectionChanged="OnMicSelectionChanged"/>
                                    </StackPanel>
                                </Grid>

                                <Separator Background="{DynamicResource BorderBrush}" Margin="0,0,0,15"/>

                                <!-- Levels -->
                                <TextBlock Text="Real-time Volume Levels (WinMM)" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource MutedText}" Margin="0,0,0,10"/>
                                
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="50"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Desk Mic" FontSize="12" VerticalAlignment="Center" Foreground="{DynamicResource Foreground}"/>
                                    <ProgressBar x:Name="DeskLevelBar" Grid.Column="1" Height="14" Minimum="0" Maximum="100" Value="0" Foreground="{DynamicResource AccentColor}" Background="{DynamicResource Background}" BorderThickness="0"/>
                                    <TextBlock x:Name="DeskLevelValue" Grid.Column="2" Text="0.0%" FontSize="12" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource MutedText}"/>
                                </Grid>

                                <Grid Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="50"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Headset Mic" FontSize="12" VerticalAlignment="Center" Foreground="{DynamicResource Foreground}"/>
                                    <ProgressBar x:Name="HeadsetLevelBar" Grid.Column="1" Height="14" Minimum="0" Maximum="100" Value="0" Foreground="{DynamicResource SecondaryAccentColor}" Background="{DynamicResource Background}" BorderThickness="0"/>
                                    <TextBlock x:Name="HeadsetLevelValue" Grid.Column="2" Text="0.0%" FontSize="12" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource MutedText}"/>
                                </Grid>

                                <Separator Background="{DynamicResource BorderBrush}" Margin="0,0,0,15"/>

                                <!-- Auto-switch Toggle -->
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="Automatic Switching (Auto-Distance)" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource Foreground}"/>
                                        <TextBlock Text="Switches to the closest mic based on speech levels" FontSize="11" Foreground="{DynamicResource MutedText}"/>
                                    </StackPanel>
                                    <CheckBox x:Name="AutoSwitchToggle" Grid.Column="1" VerticalAlignment="Center" Checked="OnAutoSwitchToggled" Unchecked="OnAutoSwitchToggled" IsChecked="False" Style="{StaticResource ToggleSwitchStyle}"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </Grid>
                </TabItem>

                <!-- Tab 2: Settings -->
                <TabItem>
                    <Border Padding="15" CornerRadius="8" Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Application Settings" FontWeight="Bold" FontSize="16" Foreground="{DynamicResource Foreground}" Margin="0,0,0,15"/>
                            
                            <!-- Notifications Toggle -->
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="Show Notifications" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource Foreground}"/>
                                    <TextBlock Text="Display HUD alert on state change" FontSize="11" Foreground="{DynamicResource MutedText}"/>
                                </StackPanel>
                                <CheckBox x:Name="NotificationsToggle" Grid.Column="1" VerticalAlignment="Center" Checked="OnNotificationsToggled" Unchecked="OnNotificationsToggled" IsChecked="True" Style="{StaticResource ToggleSwitchStyle}"/>
                            </Grid>

                            <Separator Background="{DynamicResource BorderBrush}" Margin="0,0,0,15"/>

                            <!-- Theme Selection Toggle -->
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="Dark Mode Theme" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource Foreground}"/>
                                    <TextBlock Text="Switch between Dark and Light UI styles" FontSize="11" Foreground="{DynamicResource MutedText}"/>
                                </StackPanel>
                                <CheckBox x:Name="ThemeToggle" Grid.Column="1" VerticalAlignment="Center" Checked="OnThemeToggled" Unchecked="OnThemeToggled" IsChecked="True" Style="{StaticResource ToggleSwitchStyle}"/>
                            </Grid>

                            <Separator Background="{DynamicResource BorderBrush}" Margin="0,0,0,15"/>

                            <!-- Keyboard Shortcuts Info -->
                            <TextBlock Text="Global Keyboard Shortcuts" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource Foreground}" Margin="0,0,0,8"/>
                            
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Toggle Mute:" FontSize="12" Foreground="{DynamicResource MutedText}"/>
                                <TextBlock Grid.Column="1" Text="Ctrl + Alt + M" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource Foreground}"/>
                            </Grid>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Cycle Microphone:" FontSize="12" Foreground="{DynamicResource MutedText}"/>
                                <TextBlock Grid.Column="1" Text="Ctrl + Alt + S" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource Foreground}"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</Window>
